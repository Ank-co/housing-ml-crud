# Fichier conservé pour compatibilité, le projet utilise désormais uv (pyproject.toml).
# Pour installer les dépendances, préférer uv sync
streamlit>=1.35.0
scikit-learn>=1.5.0
pandas>=2.2.0
joblib>=1.4.0
matplotlib>=3.9.0
